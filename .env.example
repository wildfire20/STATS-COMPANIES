# Database Configuration (Required)
# Railway will auto-generate this when you add a PostgreSQL plugin
DATABASE_URL=postgresql://user:password@host:port/database

# Node Environment
NODE_ENV=production

# Port (Railway sets this automatically)
PORT=5000

# Session Secret (Generate a random string)
SESSION_SECRET=your-random-session-secret-here

# Email Service (Resend API)
RESEND_API_KEY=re_your_api_key_here

# Object Storage (Optional - for image uploads)
# These are provided by Replit Object Storage
# For Railway, you'll need to configure alternative storage (e.g., AWS S3, Cloudinary)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=
# PRIVATE_OBJECT_DIR=
# PUBLIC_OBJECT_SEARCH_PATHS=
